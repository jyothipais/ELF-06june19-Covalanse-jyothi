/* function for calculating the given values*/
function calC(c) {
    //form.panel.value is used to store the value given by the user
    form.panel.value = form.panel.value + c;
}
function CE() {
    //clearing the text field
    form.panel.value = "";
}